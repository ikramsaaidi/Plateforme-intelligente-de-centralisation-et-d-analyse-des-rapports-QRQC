/**
 * Dashboard
 * -----------------------------------------------------------------------
 * Page temporaire.
 *
 * Elle sert uniquement à valider que le Layout (Sidebar + Navbar +
 * zone de contenu) fonctionne correctement via <Outlet />.
 * Le contenu réel du Dashboard sera développé dans une étape suivante.
 */
function Dashboard() {
  return (
    <h1 className="text-2xl font-semibold text-[#1F2937]">Dashboard</h1>
  );
}

export default Dashboard;